<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/selectableItemBackground">

    </FrameLayout>

    <FrameLayout
        android:id="@+id/image_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/imageViewItem"
            android:layout_width="match_parent"
            android:layout_height="@dimen/image_icon_size"
            android:layout_gravity="center"
            android:layout_marginLeft="@dimen/image_icon_margin"
            android:layout_marginRight="@dimen/image_icon_margin"
            android:layout_marginTop="@dimen/image_icon_margin"
            android:layout_marginBottom="@dimen/image_icon_bottom_margin"
            android:scaleType="centerInside"
            tools:ignore="ContentDescription"
            tools:src="@drawable/empty_image" />

    </FrameLayout>

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="1"
        app:layout_constraintTop_toBottomOf="@id/image_container"
        tools:text="@string/open" />


</androidx.constraintlayout.ConstraintLayout>